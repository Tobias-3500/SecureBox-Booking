### Application environment (copy to .env and adjust for production)

# General
NODE_ENV=production

# Database (PostgreSQL)
POSTGRES_USER=salon_user
POSTGRES_PASSWORD=change-this-password
POSTGRES_DB=salon_db

# Backend database connection (inside Docker network)
DB_HOST=db
DB_PORT=5432
DB_USER=${POSTGRES_USER}
DB_PASSWORD=${POSTGRES_PASSWORD}
DB_NAME=${POSTGRES_DB}

# Backend service
BACKEND_PORT=3001

# CORS / frontend origin
# For local development:
CORS_ORIGIN=http://localhost:3000
# For production (example):
# CORS_ORIGIN=https://your-salon-domain.dk

# JWT secret for signing auth tokens
# Use a long, random string in real deployments.
JWT_SECRET=change-this-to-a-long-random-secret

# Admin account (email that can access /admin)
ADMIN_EMAIL=secureboxadminUCL@gmail.com

# Backup VM and script (admin area: system status + manual backup)
BACKUP_VM_HOST=10.0.0.1
BACKUP_SCRIPT_PATH=/root/backup.sh
BACKUP_LOG_PATH=/var/log/backup.log

